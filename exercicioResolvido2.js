// criando um array de 5 números

const numeros = [0, 3, 18, 19, 13];
let indce = 0

// enquanto o indice for menor que o tamanho do array 
while (indce < numeros.length) {
    //executar isso
    console.log(numeros[indce]);
    indce++;
}